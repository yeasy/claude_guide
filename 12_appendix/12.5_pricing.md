# 附录 E：Claude 定价与成本参考

> **免责声明**：AI 模型定价调整频繁，本文档数据仅供参考（最后更新于 2026 年初）。做预算决策时，**必须** 以 [Anthropic 官方定价页面](https://www.anthropic.com/pricing) 为准。

---

## 12.5.1 计费模式概览

Claude API 主要采用 **Pay-as-you-go (按用量付费)** 模式，计费单位为 **每百万 Token (Per Million Tokens, PMT)**。

### 核心计费项
1.  **输入 Token (Input Tokens)**：你发送给模型的文字、图片、文档。
2.  **输出 Token (Output Tokens)**：模型生成的回复内容。通常输出 Token 的单价远高于输入 Token（约为 3-5 倍）。
3.  **提示缓存写入 (Prompt Caching Write)**：将常用 Context 存入缓存时的费用（通常比普通输入略贵）。
4.  **提示缓存读取 (Prompt Caching Read)**：复用缓存内容时的费用（通常只有普通输入的 10% 甚至更低，这是**省钱的关键**）。

---

## 12.5.2 模型定价参考表 (2026 Q1)

| 模型系列 | 输入 (Input) / PMT | 输出 (Output) / PMT | 缓存读取 (Read) / PMT | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| **Claude 3 Haiku** | $0.25 | $1.25 | $0.03 | 高并发日志分析、简单分类、翻译 |
| **Claude 3.5 Sonnet** | $3.00 | $15.00 | $0.30 | 代码生成、复杂推理、通用 Agent |
| **Claude 3 Opus** | $15.00 | $75.00 | $1.50 | 极端复杂的创意写作、科研推理 |

> 💡 **提示**：Sonnet 的性价比通常使得它成为大多数生产环境的默认选择。

---

## 12.5.3 成本估算示例

### 场景 A：代码审查助手
*   **任务**：每天审查 100 次代码提交，每次提交平均 2000 Tokens (输入)，模型回复 500 Tokens (输出)。
*   **模型**：Claude 3.5 Sonnet
*   **计算**：
    *   输入日成本：100 * 2000 / 1,000,000 * $3.00 = $0.60
    *   输出日成本：100 * 500 / 1,000,000 * $15.00 = $0.75
    *   **每日总计**：$1.35
    *   **每月总计**：~$40.00

### 场景 B：海量文档归档 (使用 Haiku + Caching)
*   **任务**：分析 1000 份合同，每份合同 50k Tokens。其中 40k Tokens 是通用的法律条款模板 (可缓存)。
*   **模型**：Claude 3 Haiku
*   **计算 (未缓存)**：1000 * 50k / 1m * $0.25 = $12.50
*   **计算 (使用缓存)**：
    *   缓存写入 (1次)：40k / 1m * $0.30 (假设写入价) ≈ $0.01
    *   缓存读取 (999次)：999 * 40k / 1m * $0.03 ≈ $1.20
    *   差异内容 (1000次)：1000 * 10k / 1m * $0.25 = $2.50
    *   **总计**：~$3.71 (**节省约 70%**)

---

## 12.5.4 外部资源

*   [Anthropic Pricing Page](https://www.anthropic.com/pricing)
*   [Claude Tokenizer (计算 Token 数量)](https://anthropic.com/tokenizer)
